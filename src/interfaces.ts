export interface Character {
  name: string;
  id: number;
}

export interface Episode {
  name: string;
  episode: string;
  id: number;
}

export interface Location {
  name: string;
  dimension: string;
  id: number;
}